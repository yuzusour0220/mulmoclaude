// `.` entry — server-facing core (definition + types + executeHtml against the
// generic gui-chat-protocol `files.artifacts` capability). The Vue View lands
// in a `./vue` entry in phase 2 (see plans/feat-presenthtml-mulmoterminal.md).
export * from "./core/index";
