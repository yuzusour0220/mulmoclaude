{
  "name": "@mulmobridge/webhook",
  "version": "0.1.1",
  "description": "Generic webhook bridge for MulmoBridge — POST JSON, get the AI reply in the response. Any HTTP-speaking tool can use it.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "mulmobridge-webhook": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "prepack": "yarn build",
    "typecheck": "tsc --noEmit",
    "test": "echo no tests yet"
  },
  "license": "MIT",
  "author": "Receptron Team",
  "dependencies": {
    "@mulmobridge/client": "^0.1.4",
    "@mulmobridge/protocol": "^0.1.4",
    "dotenv": "^17.0.0",
    "express": "^5.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^25.8.0",
    "typescript": "^6.0.3"
  }
}
