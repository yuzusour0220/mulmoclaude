{
  "name": "@mulmobridge/xmpp",
  "version": "0.1.1",
  "description": "XMPP / Jabber bridge for MulmoBridge — connect any XMPP server (ejabberd, Prosody, etc.) to MulmoClaude",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "mulmobridge-xmpp": "./dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "prepack": "yarn build",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/test_*.ts"
  },
  "license": "MIT",
  "author": "Receptron Team",
  "dependencies": {
    "@mulmobridge/client": "^0.1.4",
    "@mulmobridge/protocol": "^0.1.4",
    "@xmpp/client": "^0.14.0",
    "dotenv": "^17.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
