// `resolveEnumColor` moved into @mulmoclaude/collection-plugin (collection
// frontend extraction). This thin re-export keeps the host Collection*
// components compiling until they move into the package too — it goes away with
// them.
export { resolveEnumColor, type EnumColorClasses } from "@mulmoclaude/collection-plugin";
