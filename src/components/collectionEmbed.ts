// The `embed` field view-model types (EmbedRow / EmbedView) moved into
// @mulmoclaude/collection-plugin (collection frontend extraction). Re-exported
// here so the host Collection* components keep importing them unchanged.
export type { EmbedRow, EmbedView } from "@mulmoclaude/collection-plugin";
